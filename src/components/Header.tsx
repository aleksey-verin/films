import React from 'react'

const Header = () => {
  return (
    <header>
      <div className="wrapper">
        <nav>
          <button>Home</button>
          <button>Login</button>
        </nav>
      </div>
    </header>
  )
}

export default Header
